# emstime


Setup: (Arch)
- Must have Python and Node installed
- Install UV with `curl -LsSf https://astral.sh/uv/install.sh | sh`
- Install Pebble SDK with `uv tool install pebble-tool --python 3.13`

Build and Run:
- `pebble build`
- `pebble install --emulator basalt`